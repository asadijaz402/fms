import { useContext } from "react";
import BuySlotDialogContext from "../context/BuySlotDialogContext";

const useBuySlotDialiog = () => useContext(BuySlotDialogContext);

export default useBuySlotDialiog;
